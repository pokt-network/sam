services:
  sam:
    build: .
    ports:
      - "9999:9999"
    volumes:
      - ./config.yaml:/app/config.yaml
      - sam-data:/app/data
    environment:
      - PORT=9999
      - BIND_ADDR=0.0.0.0
      - CONFIG_FILE=/app/config.yaml
      - DATA_DIR=/app/data
      # - ALLOWED_ORIGINS=https://sam.example.com
      # - AUTH_TOKEN=your-secret-token-here
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9999/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped

volumes:
  sam-data:
